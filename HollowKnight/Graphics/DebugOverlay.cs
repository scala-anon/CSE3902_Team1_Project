using Microsoft.Xna.Framework.Graphics;

namespace HollowKnight.Graphics
{
    // TODO: Consolidate all debug drawing from Game1.Draw() into this class.
    // Should own: DrawRectangleOutline, all DebugRenderer calls for hitboxes/state labels/paths,
    // the grid toggle, and the hitbox toggle.
    // Game1.Draw() should call debugOverlay.Draw(spriteBatch) once instead of
    // having debug rendering scattered throughout.
    // Controlled by a single bool ShowDebug flag.
    public class DebugOverlay
    {
    }
}
